{
  "name": "@leebmann24/recursive-yaml-loader",
  "version": "1.0.7",
  "description": "recursively loads yaml files from a given folder and it's subfolders. After that it merges everything into a single json object which will be returned.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/leebmann24/recursive-yaml-loader"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest"
  },
  "keywords": ["Leebmann24", "yaml", "configuration", "config", "load", "recursive"],
  "author": "Bernhard Schönberger (bernhard.schoenberger@leebmann.de)",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.7",
    "@types/jest": "^27.0.2",
    "@types/js-yaml": "^4.0.4",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  }
}
